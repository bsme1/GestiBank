<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.gestibank"
    android:targetSandboxVersion="1">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GestiBank"
        android:usesCleartextTraffic="true">
        <activity android:name=".Client"></activity>
        <activity android:name=".Agent" />
        <activity android:name=".ListeAgents" />
        <activity android:name=".ListeDemandes" />
        <activity android:name=".AjoutAgent" />
        <activity android:name=".Admin" />
        <activity android:name=".Conversion" />
        <activity android:name=".Authentification" />
        <activity android:name=".CreationCompte" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>